import { OrderByRankingPipe } from './order-by-ranking.pipe';

describe('OrderByRankingPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByRankingPipe();
    expect(pipe).toBeTruthy();
  });
});
